# Optional: copy to experiments/so101_trainswap/env/pi.env for local shell helpers (gitignored).

# SO-101 serial device on Pi
ROBOT_PORT=/dev/ttyACM1

# Primitive HTTP server bind
PRIMITIVE_HOST=0.0.0.0
PRIMITIVE_PORT=8765
